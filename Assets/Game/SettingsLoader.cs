namespace Game
{
    public class SettingsLoader
    {
        public GameSettings LoadSettings()
        {
            GameSettings settings = new GameSettings();
            
            return settings;
        }
    }
}
